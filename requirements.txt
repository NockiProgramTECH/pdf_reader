# Dépendances du projet Lecteur PDF Vocal

# Interface graphique
customtkinter==5.2.1

# Traitement PDF
PyPDF2==3.0.1

# Synthèse vocale (hors ligne)
pyttsx3==2.90

# Dépendances système pour pyttsx3 (Windows):
# - pywin32 (installé automatiquement avec pyttsx3)

# Dépendances système pour pyttsx3 (Linux):
# - espeak: sudo apt-get install espeak
#python3-espeak #pip install python3-espeak

# Dépendances système pour pyttsx3 (macOS):
# - Utilise le moteur natif NSSpeechSynthesizer (aucune installation nécessaire)

# Bibliothèques optionnelles pour amélioration (en ligne):
 gtts==2.4.0  # Google Text-to-Speech (nécessite connexion Internet)
 playsound==1.3.0  # Pour lire les fichiers audio générés par gTTS